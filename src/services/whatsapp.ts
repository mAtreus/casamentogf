export async function sendWhatsAppMessage(phone: string, name: string) {
  // Implement your WhatsApp service integration here
  // This is a mock implementation
  console.log(`Sending WhatsApp message to ${phone}`);
  return Promise.resolve();
}